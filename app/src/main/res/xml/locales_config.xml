<?xml version="1.0" encoding="utf-8"?>
<!--
  Architecture v2.1.0 §29 + 헌법 §8-2 — UI 언어 사용자 선택 옵션.
  Stage 2-009 (PR #30) 신설.

  본 PR은 Locale 적용 인프라(AppCompatDelegate.setApplicationLocales)를 명문화.
  실제 strings.xml values-xx 번역 추가는 점진 확장 (별도 PR).
  현재 fallback: en (만국 공통).
-->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="ko" />
</locale-config>
