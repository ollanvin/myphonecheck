<?xml version="1.0" encoding="utf-8"?>
<!-- v4.3 DEBUG ONLY — not included in release builds -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="app.myphonecheck.mobile.debug.DebugSearchActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="app.myphonecheck.mobile.debug.SEARCH_TEST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
