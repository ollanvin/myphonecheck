package app.callcheck.mobile.feature.decisionui.di

import dagger.Module
import dagger.hilt.InstallIn
import dagger.hilt.components.SingletonComponent

/**
 * Hilt dependency injection module for Decision UI feature.
 *
 * This module is currently a placeholder for future DI requirements.
 * The main dependencies (DecisionEngine) are provided by other feature modules.
 */
@Module
@InstallIn(SingletonComponent::class)
object DecisionUiModule {
    // Future DI bindings can be added here
}
